app_name: "{{ app_name }}"
environment: "{{ env }}"
version: "{{ version }}"

database:
  host: "{{ db_host }}"
  port: "{{ db_port }}"
  name: "{{ db_name }}"
  user: "{{ db_user }}"
  password: "{{ db_password }}"